// import stdio

// test const decl
const int scope = 0; 

// test function decl
void func main() {
    // test field decl
    string s = "123";
    string es = "";
    char c = '1';
    // string ws = "12;
    // char wc = '1;
    // char ec = '';
    // this is a comment
    //
    s += "e";
    es += "eps";
    bool b = true;
    b = false;

    // ~?

    int func(float, bool) foo = int (float f, bool b) -> {
        f= 0.489;
        ++f--;
        // float _wf = 1.1.;
        // float wf1 = 1.;
        // int b = 1;
        return f + 1.0 / 9.0;
    };
    
    float t = 0.0;
    foo(t, true);
    foo(1.1, false);
    // foo("1");

    for (;;) {
        foo(1.1, false);
        foo(1.1.(float), false);
    }

    for (int i = 0; i <= 10; i++, i = foo(i.(float), true)) {
        if (t >= foo(t, true).(float) && t > 0.0) {
        }
    }

    while (t >= 0.0 || !b) {
    }

    switch (b) {
        case true: {}
        case false: {}
        default: {}
    }
    int[] func(int, float)[] xx;
    int w;
    while ((xx = bar(b)) != null && w >= 0) {
        w <<= (3 % ( 2 * 10 - 1 ));
    }

    int[] func(int, float)[] func(bool) bar = int[] func(int, float)[] (bool b) -> {
        
    };

    int[] arr = new int[4];
    int[] array = new int[2] {
        // x, "string", 1.1, 5, 'a', { t, bar(), { sizeof(bar(false)) } }
        w, 1, (1.1).(int), sizeof(arr)
    };

    if (true);

    int[] h = arr;

    int[] x = arr[:];
    int[] y = arr[1:];
    int[] z = arr[1:5];
    int[] d = arr[:5];
    int l = arr[1];
    int m = arr[foo(l.(float), true)];
    int size = sizeof(z);

    int[][] map = new int[2][2] {
        {2, 3}, 
        {4, 5}
    };
}

int func str(string s) {

}